import SwiftUI


struct ContentView: View {
//    @State var
    var body: some View {
        ZStack{
            flipcard()
                .frame(width: 300, height: 200)
            
        }
    }
}
